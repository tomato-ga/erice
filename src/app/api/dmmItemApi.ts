import { DMMItem, DMMItemDetails } from '../types/dmmTypes';

export async function fetchData(cid: string): Promise<DMMItem> {
  // fetchData関数の実装
}

export async function fetchitembycontentid(contentid: string): Promise<DMMItemDetails> {
  // fetchitembycontentid関数の実装
}